version: 2
project_name: nst

before:
  hooks:
    - go mod tidy
    - go test ./...

builds:
  - id: nst
    main: ./cmd/nst
    binary: nst
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X github.com/alihamzaops/namespace-terminator/internal/cli.version={{.Version}}

archives:
  - id: default
    ids:
      - nst
    formats:
      - tar.gz
    format_overrides:
      - goos: windows
        formats:
          - zip
    name_template: >-
      {{ .ProjectName }}_
      {{- .Version }}_
      {{- .Os }}_
      {{- .Arch }}

checksum:
  name_template: checksums.txt

snapshot:
  version_template: "{{ incpatch .Version }}-next"

changelog:
  use: git
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"

release:
  github:
    owner: alihamzaops
    name: namespace-terminator

brews:
  - name: nst
    ids:
      - default
    repository:
      owner: alihamzaops
      name: homebrew-tap
      branch: main
      token: "{{ .Env.TAP_GITHUB_TOKEN }}"
    directory: Formula
    homepage: https://github.com/alihamzaops/namespace-terminator
    description: Force terminate stuck Kubernetes namespaces
    license: MIT
    test: |
      system "#{bin}/nst", "--help"
    install: |
      bin.install "nst"

scoops:
  - name: nst
    ids:
      - default
    repository:
      owner: alihamzaops
      name: scoop-bucket
      branch: main
      token: "{{ .Env.SCOOP_GITHUB_TOKEN }}"
    homepage: https://github.com/alihamzaops/namespace-terminator
    description: Force terminate stuck Kubernetes namespaces
    license: MIT
